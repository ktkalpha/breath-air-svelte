import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sum.ktkalpha',
  appName: '숨',
  webDir: 'build',
  server: {
    androidScheme: 'https'
  }
};

export default config;
